# 幕海 MuHai V3.5.7 更改清单（草稿 · 待动手）

> 本清单对应 2026-09-20 反馈的 4 处 UI 问题 + F 系列功能（F6/F7）。**当前仅完成代码定位与改法分析，尚未改源码、未构建。**
> 版本号已先行升到 `3.5.7`（package.json / src-tauri/tauri.conf.json / src-tauri/Cargo.toml 三处已同步，未提交）。
> F 系列范围已确认：**F6 直播时移/回看（底时间轴）、F7 投屏进场自动轮询（UI 不变）**；F8/F11 移除；F9/F10 复用现有界面、不变。

---

## 问题 1 · 直播横屏底部工具栏空隙太大

**现象**：V3.5.6 把直播横屏底部按钮从竖排改回横排（✅ 图标对了），但 4 个按钮两端留白大、中间很松（图 2），要改成旧版那种紧凑撑满（图 1）。

**根因 / 代码位置**
- 直播横屏底部结构：`src/video/views/Live.tsx` 第 40 行 `<div className="land-overlay landscape"><div className="land-bottom">…4 个 .bb 按钮…</div></div>`。
- 共用规则：`src/styles.css` 第 1982 行
  ```css
  .landscape .land-bottom { … justify-content: space-between; gap: 8px; padding: 10px 18px …; }
  ```
  `space-between` 把 4 个按钮往两端推开 → 中间空隙大。点播横屏（9 个按钮）用 space-between 没问题，**但直播横屏只有 4 个**，就显得空。

**改法（仅作用于直播横屏，不破点播）**
- 新增覆盖规则，限定直播场景（`.land-overlay.landscape`）：
  ```css
  .land-overlay.landscape .land-bottom { justify-content: center; gap: 26px; padding: 10px 16px calc(env(safe-area-inset-bottom,0px) + 10px); }
  ```
  4 个按钮居中、固定间距，紧凑不撑满，对齐图 1。点播横屏仍走 `space-between` 不变。

---

## 问题 2 · 点播竖屏「上一集 / 下一集」尺寸纠正（与之前要求相反）

**现象**：你这次纠正了上一轮的要求 ——
- **上一集 / 下一集** 圈 = 与**左中弹幕圈一样大**（38px）
- **中间暂停按钮** 比上一集/下一集 **大一点**（如 44~48px）

> 注意：V3.5.6 做的是「暂停 38px、上下集 32px」，与本次要求**正好相反**，需改回去。

**代码位置**（`src/styles.css`）
- 左中弹幕圈规格：`vp-side .side-btn` 第 725 行 → **width/height 38px**（基准值）。
- 竖屏中央上一集/下一集（小钮）：第 1776 行 `.player-card .center button.ctrl.small` → 现 **32px**。
- 竖屏中央暂停（大钮）：第 1790 行 `.player-card .center .big-btn` → 现 **38px**。

**改法**
```css
/* 上一集/下一集：32 → 38px（= 左中弹幕圈） */
.player-card .center button.ctrl.small {
  width: 38px !important; height: 38px !important;
  min-width: 38px !important; min-height: 38px !important;
  max-width: 38px !important; max-height: 38px !important;
}
.player-card .center button.ctrl.small svg { width: 20px !important; height: 20px !important; }

/* 暂停：38 → 46px（比上下集大一圈） */
.player-card .center .big-btn {
  width: 46px !important; height: 46px !important;
  min-width: 46px !important; min-height: 46px !important;
  max-width: 46px !important; max-height: 46px !important;
}
.player-card .center .big-btn svg { width: 22px !important; height: 22px !important; }
```
> 暂停取 46px（比 38 大 8px，明显大一圈，又不至于抢戏）。若你想要更大可改 48px。

---

## 问题 3 · 详情页卡片：影视名字 / 加入收藏按钮上下突出卡片

**现象**：详情卡（图 3）里 `影视名字` 顶部突出卡片上沿、`加入收藏` 按钮底部突出卡片下沿；要对齐原型（图 5）—— 标题与按钮都收在卡片边界内。

**代码位置**（`src/video/VideoPlayer.tsx` 详情信息区 + `src/styles.css`）
- 结构：`VideoPlayer.tsx` 第 1592 行起 `.info-card` → `.info-poster` + `.info-body`（含 `.info-title` / `.info-sub` / `.fav-btn`）。
- `.info-card`：第 1850 行 `display:flex; gap:14px; align-items:stretch;`（背景透明，视觉卡片由 `.info-poster` 84×112 与信息区共同构成）。
- `.info-body`：第 1854 行 `flex-direction:column; justify-content:space-between;` → 把 title 顶到最上、fav 压到最底。
- `.info-title`：第 1855 行 `font-size:17px; font-weight:800; line-height:1.3;`（标题一长就 2 行，高度顶出）。
- `.fav-btn`：第 1866 行 `align-self:flex-start; margin-top:10px;`（`space-between` 下被推到最底，偶尔溢出）。

**改法**
- `.info-title`：限制最多 2 行并裁切，防止顶出卡片：
  ```css
  .info-title { font-size: 17px; font-weight: 800; line-height: 1.3; margin: 0;
    display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }
  ```
- `.info-body` / `.fav-btn`：去掉生硬的 `space-between` 顶底，改为自然堆叠 + fav 固定在底但不溢出：
  ```css
  .info-body { gap: 6px; justify-content: flex-start; }
  .fav-btn { align-self: flex-start; margin-top: 2px; }
  ```
  （标题在上、信息居中、收藏按钮紧随信息之后，整体不超出 poster 112px 高度范围。）

---

## 问题 4 · 详情页中间信息（主演/导演）超长 → 用「…」截断

**现象**：中间信息（年份·类型·导演·主演）过长时整段铺开、把卡片撑高；要对齐原型，超长用省略号收尾。

**代码位置**
- JSX：`VideoPlayer.tsx` 第 1616 行 `.info-sub`，主演用 `<br/>` 后整段展示，无裁切。
- 样式：第 1856 行 `.info-sub { font-size:12px; color:var(--muted); margin-top:6px; line-height:1.6; }`。

**改法**：限制 2 行并省略号（保留「年份·类型·导演」整行 + 「主演」最多一行截断）：
```css
.info-sub { font-size: 12px; color: var(--muted); margin-top: 6px; line-height: 1.6;
  display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }
```
> 若原型只截「主演」一行，可改为只给主演那一段套 line-clamp（需在 JSX 把主演单独包一层 span）。本方案先整体 2 行截断，最贴合「信息多了用…表示」。

---

## 问题 5 · F6 直播时移 / 回看（核心新功能）

**现象**：直播播放器目前**没有时间轴 / 回看能力** —— 横屏底部只有 `换源 / 画中画 / 播放 / 选台` 4 个按钮，无法回看已播内容。需新增一条底部时间轴，支持「拖回去回看 + 一键回到直播」。

**入口（点击点）**：转手机横看后的直播横屏，
1. **底部时间轴**（4 个工具按钮上方那条进度条）—— 点非直播位置 / 拖动圆点往回拉 → 进入回看，画面左上出现 `⏪ 回看中 12:30` 标签，跳到对应时间点；
2. **画面右上「回到直播 ›」按钮** —— 点一下跳回直播边缘，回看标签消失。

**代码位置**（`src/video/views/Live.tsx` 直播横屏 + 直播播放器状态机）
- 横屏底部结构：`Live.tsx` 第 40 行 `<div className="land-overlay landscape"><div className="land-bottom">…4 个 .bb 按钮…</div></div>`（**无 `.land-progress` 进度条**）。
- 直播播放器状态：需在 `Live.tsx` 现有 player 状态里扩展「回看偏移 / 是否直播态」。

**改法**
- 在 `.land-bottom` 上方（或并入横屏底部区）新增时间轴 `.ts-bar` + 直播小圆点 `.ts-live` + 回看刻度 `.ts-tick`：
  - 直播态：进度条满、圆点在最右、`回到直播` 按钮隐藏；
  - 点击/拖动非直播点：`player.seek(目标时间)` 进入回看，置 `isReplay=true` 并顶上 `回看中 HH:MM` 标签；
  - `回到直播` 按钮：`isReplay=false`、seek 到直播边缘、隐藏标签。
- **回看范围取决于源是否支持时移切片**：支持则可在整个节目时长内回看；不支持（纯 HLS 直播无切片）则只能回看本地缓冲的一小段（缓冲耗尽即到底）。先做「本地缓冲回看 + 回到直播」，时移切片作为后续按源能力开启。

---

## 问题 6 · F7 投屏进场自动轮询（UI 不变，仅行为增强）

**现象**：`CastOverlay` 进场只扫描一次（`useEffect(scan, [])`），弱网 / 设备晚开机时容易「搜不到」。需改为进场后自动轮询，且不改动任何界面结构。

**代码位置**：`src/components/CastOverlay.tsx`
- 第 35 行 `useEffect(() => { scan(); }, [scan]);` —— 一次性扫描。
- 第 21-33 行 `scan()` 调 Rust 后端 `dlnascan` 扫描局域网，成功写入 `devices`，为空则提示。

**改法（界面 0 改动）**
- 保留现有弹窗结构不变：`.modal-mask > .modal > .modal-title[投屏设备] + .cast-list(.dev: 电视图标 + 名称 + DLNA 徽标) + .modal-btns[重新搜索 / 取消]`。
- 仅把「进场一次性扫描」改为「进场自动轮询」：
  - `scan()` 增加 `setInterval` 定时重扫（如每 5s 一次），直到搜到设备 / 用户点「取消」/ 手动「重新搜索」时清掉定时器；
  - `loading` 态文案可保持「正在搜索局域网设备…」，设备出现后列表实时刷新；
  - 不新增任何按钮、不新增任何样式类。
- 行为增强后 UI 与现有 `CastOverlay` 完全一致。

---

## 改动文件汇总（预期）

| 文件 | 改动 |
|---|---|
| `src/styles.css` | ① 直播横屏 `.land-overlay.landscape .land-bottom` 居中紧凑；② 竖屏 `.ctrl.small` 32→38px、`.big-btn` 38→46px；③④ 详情 `.info-title`/`.info-body`/`.fav-btn`/`.info-sub` 裁切与对齐 |
| `src/video/VideoPlayer.tsx` | ④（`info-sub` 若改为单行截断需调整 JSX，可选） |
| `src/video/views/Live.tsx` | ⑤ F6 直播横屏底部新增时间轴 `.ts-bar` + 回看/`回到直播` 逻辑（状态机扩展 `isReplay` / 回看偏移） |
| `src/components/CastOverlay.tsx` | ⑥ F7 `scan()` 由一次性改为进场自动轮询（UI 0 改动，弹窗/设备列表/DLNA 徽标/按钮全保留） |

## 验证计划（动手时执行）
1. `npx tsc --noEmit` 类型检查。
2. Playwright 真机渲染：直播横屏底部 4 键间距紧凑（图 1 对照）；竖屏中央三钮 上一集/下一集 38px、暂停 46px；详情卡片标题与收藏不溢出、主演 2 行省略。
3. `pnpm build` → 提交 → tag `v3.5.7` → 触发 CI 出包。

## 注
- 版本号已升 `3.5.7`，但 **源码改动与构建尚未进行**，等你确认清单后动手。
