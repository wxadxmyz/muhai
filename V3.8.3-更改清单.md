# V3.8.3 更改清单 — 精简到「苹果 CMS + drpy」并修复播放链路

> **决策背景**：用户明确只要 **苹果 CMS（`normal` 型）** 与 **drpy** 两类源，放弃整个影视仓 csp 体系（含 JS 型 csp 与 Phase 2 的原生 DEX 蜘蛛源）。
> 据此本次分两件事：① 把 csp 体系从代码里干净移除；② 修复即使只用苹果 CMS/drpy 也仍然存在的播放链路 bug（m3u8 崩溃、云线路转圈）。

---

## 一、范围判定（删什么 / 留什么）

| 模块 | 归属 | 处置 |
|---|---|---|
| `patch-csp-android.sh` + Kotlin `MuHaiCsp`/`CspNative`/`Init`/`Java` 桥 | Phase 2 原生 DEX | **整删**（CI 同步去掉调用行） |
| `js_engine.rs` 的 `__native_csp` 分支、catvod `java` 兼容层、csp 管理器下载/md5 | V3.8.0/3.8.1 csp | **删分支**（保留 QuickJS 沙箱与 drpy2 引擎核心） |
| `tvbox.ts` 的 `isCspSite` / `spiderField` / csp 选路 | V3.8.0 csp | **删** |
| `js.ts` 的 `MuHaiCsp.require` 路由 | Phase 2 csp | **删** |
| `types.ts` 的 `CspSourceConfig` / `'csp'` 类型 | V3.8.0 csp | **清理**（接口删，类型枚举保留空值避免序列化破坏） |
| `drpy3.rs` / `drpy3.ts`（drpy 引擎本体） | drpy | **保留**（与 csp 无关） |
| 本地流式媒体代理（127.0.0.1）、`normal.ts`、`hlsPlayer.ts` | 播放链路 | **保留 + 修**（苹果 CMS/drpy 播放 m3u8 都依赖） |

> 苹果 CMS 自身就带「云」等多线路（HTML 藏链），`normal.ts` 的 `resolvePlayUrl` 是它自己的解析逻辑，**不是** csp 专属，必须保留。

---

## 二、删除清单（csp 体系）

### 2.1 整删 Phase 2 脚本与 CI 调用
- [ ] 删除文件 `scripts/patch-csp-android.sh`（其中 `com.github.catvod.csp.Java` / `com.github.catvod.spider.Init` / `com.muhai.app.CspNative` / `MuHaiCsp` 全部随之移除）
- [ ] `.github/workflows/android.yml` 第 474 行 `run: bash scripts/patch-csp-android.sh` 删除，并移除对该脚本产物的任何引用

### 2.2 `src-tauri/src/js_engine.rs` —— 删 csp 分支，保留沙箱/drpy2 核心
- [ ] 删除 `__native_csp` 描述符返回逻辑（约 L166–L172 的 `Phase 2：Android 端交由 Kotlin…` 整段）
- [ ] 删除 csp 管理器下载 + md5 校验段（约 L150–L161）
- [ ] 删除 `catvod java 宿主兼容层`（约 L296 起的 `java` 对象注入：`stringToMap` / `getWebWaiter` / `new spider(api, ext)` 相关注释与实现）
- [ ] 移除 csp 专用字段：`CspSourceConfig` 在 Rust 侧的 `csp_api` / `csp_ext` / `spider_url` / `spider_md5`（L23–L44 一带的注释与结构体字段）
- [ ] **保留**：`spiderrun` 的 drpy2/spider JS 执行主路径；若 `java` 宿主对象被 drpy2 复用则保留（确认 drpy2 是否依赖后再定）

### 2.3 `src/engine/adapters/tvbox.ts` —— 删 csp 选路
- [ ] 删除 `spiderField()`（L80）
- [ ] 删除 `isCspSite()`（L104）
- [ ] 删除所有 `csp_` 选路分支（L127–L132、L167、L172、L191–L213 的 `if (isCspSite(s))` / `singleApi.startsWith('csp_')` / `spiderField(...)` 调用）
- [ ] 保留：`normal`/`drpy` 站点解析、多线路 `$$$` 分组、顶层 `spider` 为远程 URL 时的**非 csp** 处理（如有）

### 2.4 `src/engine/adapters/js.ts` —— 删 MuHaiCsp 路由
- [ ] 删除 `MuHaiCsp.require` 调用段（L333–L374：含 `w.MuHaiCsp?.require`、`__csp_error` 处理、`已确认是原生 DEX 源…` 分支）
- [ ] 删除 `spiderrun` 返回 `__native_csp` 后的锁定路由（L406–L408）
- [ ] 保留：drpy 源经 `drpy3` 引擎的正常路径

### 2.5 `src/engine/types.ts` —— 清理类型
- [ ] 删除 `CspSourceConfig` 接口（L38–L40 及其 csp 字段注释）
- [ ] `'csp'` 枚举值：保留字面量（避免历史配置反序列化报错），但标注为 deprecated，不再有新代码引用

---

## 三、保留并修复（播放链路）—— 即便只用苹果 CMS/drpy 也必修

### 3.1 根治 m3u8 线路 `Range: bytes=0-0` 探测崩溃（来自 09:16 / 09:19 真机日志）
**现象**：hls.js 对无 `#EXT-X-BYTERANGE` 的分片默认发 `Range: bytes=0-0` 畸形头；代理虽转全量 200，但 hls.js 仍按 1 字节解析 → `fragParsingError` → `fatal mediaError` → 转圈/卡死。

- [ ] `src/lib/hlsPlayer.ts` 的 `createBackendLoader`：当 `rangeStart === 0 && (rangeEnd === 0 || rangeEnd === '' || rangeEnd === 'NaN')` 时，**不设置 `Range` 头**，让请求变成普通 GET。代理收到无 Range → 回 200 全量 → hls.js 不按 1 字节截取
- [ ] 同步在 Rust 代理侧加一条防御：`parse_range` 对 `bytes=0-0` 继续判 `None`（当前已是），但补测：代理返回 200 时确保**不**带 `Content-Range`，避免 hls.js 误判为部分响应
- [ ] 回归验证指标：`proxy_probe_snapshot` 中 `range_dropped` 计数上升，但前端 `疑似探测响应：收到 1 字节` 应**不再出现**

### 3.2 修复苹果 CMS「云」线路 WebView 转圈（HTML 藏链）
**现象**：`v.gsuus.com/play/xxx`（无 `/index.m3u8`）返回 DPlayer HTML 网页，真实 m3u8 藏在 `<script>const vid='.../index.m3u8'` 里；当前 `resolvePlayUrl` 未抠出 → 播放器拿 HTML 一直转圈。

- [ ] `src/engine/adapters/normal.ts` 的 `resolvePlayUrl`：
  - 增加对返回 HTML 的兜底：正则匹配 `vid\s*=\s*['"]([^'"]+\.m3u8)['"]`、`player_URl`、`index\.m3u8` 等常见藏链模式
  - 解析成功后回写真实 m3u8 地址，再走 3.1 的 Range 修复
  - 解析失败时**显式报错**（而非静默把 HTML 交给播放器转圈），给出「该线路暂不支持」提示
- [ ] `fetchsource` 抓取页面时带 `Referer` + 浏览器 UA（部分源站对无 Referer 的抓取返回空/403）

### 3.3 `tvbox.ts` JSON 容错解析（保留，对苹果 CMS/drpy 配置同样有效）
- [ ] 增加宽松解析：容忍缺失引号（肥猫式 `"ext": {` 缺前引号）、尾逗号、行内注释
- [ ] 解析失败时明确提示，避免静默生成空站点列表

### 3.4 媒体代理本身保留不动
127.0.0.1 流式代理是苹果 CMS/drpy 播放 m3u8 的基础设施，本次只修 3.1/3.2 的**调用方**，不改动代理转发核心。

---

## 四、关于「清晰度」的重要说明（避免无效方向）

- 实测结论：**「云」线路与「m3u8」线路是苹果 CMS 同一条流、同一清晰度**，切换线路不会提升画质。
- 苹果 CMS 采集站的清晰度天花板 = 源站本身提供的码率；客户端切线路无法突破。
- 若用户后续要更高清晰度（网盘直链 / 4K csp），那是**新增源类型**的事，与本次删 csp 不矛盾（本次只是不内置影视仓 csp，未来仍可单独支持网盘分享 `netdiskShare`）。
- 建议：`settings.ts` 的 `autoDetectLine` 保持默认 `false`，避免无意义自动切线路。

---

## 五、验证方式

> ⚠️ 本环境无 Android 运行时，以下仅能在编译/类型层面验证；真机可用性由用户实测。

- [ ] `cargo check`（host）通过，确认 csp 分支移除后无未定义引用
- [ ] `pnpm build`（tsc + vite）通过，确认 `CspSourceConfig` / `MuHaiCsp` / `isCspSite` 等引用已清理干净
- [ ] CI Android Build 通过（已去掉 `patch-csp-android.sh` 调用）
- [ ] APK 体积较 v3.8.2 下降（不再注入 Kotlin 桥；若之前为 csp 引入的 QuickJS Android 库被一并移除，降幅更明显）
- [ ] **真机验证 A（苹果 CMS m3u8）**：播放 `gitee` 源中光速/极速资源的 m3u8 线路 → 不应再出现 `fragParsingError`/`收到 1 字节`
- [ ] **真机验证 B（云线路）**：切到「云」线路 → 应解析出真实 m3u8 播放，或明确报错而非无限转圈
- [ ] **回归（drpy）**：drpy 源搜索/播放正常，确认未误删 drpy 引擎依赖

---

## 六、风险与说明

| 风险 | 说明 | 应对 |
|---|---|---|
| 误伤 drpy | `js_engine.rs` 同时服务 drpy2/spider，删 csp 分支需确认 drpy2 不依赖 catvod `java` 兼容层 | 删前用 grep 确认 `java` 对象仅 csp 引用；drpy3 走独立 `drpy3.rs`，不受影响 |
| 历史配置 | 用户已添加过 csp 源，本地配置含 `type:'csp'` | `'csp'` 枚举保留空值，反序列化不报错，仅不再展示/加载 |
| 体积回落 | v3.8.2 为 Phase 2 引入的 Kotlin 桥使 APK 增大 | 本次移除后应回落；v3.8.3 出包后重新测体积，再定瘦身最终参数 |
| 云线路解析鲁棒性 | 不同苹果 CMS 站藏链格式不一 | 3.2 先做常见模式兜底 + 失败报错，真机补充更多样本后再扩正则 |
