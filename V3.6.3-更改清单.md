# V3.6.3 更改清单（2026-09-21）

> 目标：一次性修好"导入 drpy 源后搜不了影视"的问题——不再依赖失效/被墙源，且单个源失败不再吓人、不阻断其它源。

## 一、源仓库（gitee xmyzjxn/muhai-vod，已推送）

1. **`drpy-sources.json` 重写为 4 个国内可达活源**（均为 `var rule` 形态 drpy2 规则，由本地 drpy3 引擎承载）：
   - 豆瓣（douban.js）—— gitee raw 直连
   - 荐片（jianpian.js）—— gitee raw 直连
   - 爱看机器人（ikanbot.js）—— gitee raw 直连
   - 我的哔哩（bili.js）—— gitee 内容风控 451，改用 `ghproxy.net` 代理 github raw
2. **新增 `drpy-live-sources.json`**：虎牙 / 斗鱼 / 兔小贝 三个直播规则源从点播清单**拆分**出去（点播搜索不再误碰直播源）。
3. **删除失效的芒果规则** `rules/mgtv-dr2.js`（其搜索接口已改版需签名，执行即 `forEach of undefined`）。
4. 规则文件已实测下载可达（gitee 302→200 直出规则内容；bili 经代理 200/79KB）。

## 二、App 搜索容错（src/components/SearchView.tsx + src/styles.css）

- 单源搜索失败时，左侧源栏**不再红色"!"报警**、不再 tooltip 暴露 `cannot read property 'forEach' of undefined` 等技术栈错误，改为低调灰色「暂」标记 + 友好提示「该源暂不可用，可换其它源」。
- 主区「该源未连通 + 原始错误」大窗改为「该源暂不可用」友好提示，不泄露技术细节。
- 单源失败**不阻断其它源结果**（聚合层本就 try/catch 单源、正常源结果照常渲染），挂了的源灰掉即可。

## 三、版本

`3.6.2 → 3.6.3`：package.json / Cargo.toml / tauri.conf.json。

## 四、已知限制（诚实说明）

- 沙箱无法真跑 drpy3 引擎验证每个源"能否出片"，只能保证规则 URL 可达 + 形态兼容 + 引擎加载正确；每个源最终能否搜到片取决于外部站点当时是否活着，需真机实测。
- 容错兜底下：活着的源能用，挂掉的不挡路。
- 点播主力仍建议用 `sources.json`（16 个苹果CMS 子站，实测最稳）；`drpy-sources.json` 作为 drpy 规则源补充。
