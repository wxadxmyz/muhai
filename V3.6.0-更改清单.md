# V3.6.0 更改清单 —— P0：drpy3 / drpy2 蜘蛛源引擎

> 目标：让幕海能跑影视仓（TVBox）生态里的 **JS 规则源**（drpy2 的 `var rule = {...}`、drpy3 的 `export default {...}`）。
> 此前幕海的裸 QuickJS 沙箱只能跑「自己实现 home/search/detail/play 全局函数」的脚本，规则源缺 jsoup 选择器与加密库，一律跑空。

---

## 一、为什么选 drpy3 而不是 drpy2

| 方案 | 体积 | 依赖 | 结论 |
|---|---|---|---|
| `drpy2.min.js`（65KB） | 小 | **9 个外部依赖**（cheerio / crypto-js / pako / gbk / node-rsa / jinja / json5 / 模板 / uri），要按 `assets://` 逐一下发 | 依赖地狱，放弃 |
| `drpy-core-lite.min.js`（718KB，drpy3） | 大 | **0 外部依赖**（CryptoJS / pako / gbk / jinja / node-rsa 全部内嵌） | 自包含，采用 |
| `drpy3.js` 引擎 + 上述库包 + cheerio 版 jsoup | 1.7MB（打包后） | 0 | **drpy3 自带 `load2x` 兼容层，drpy2 老源零改动运行 → 一条路吃两家** |

drpy3 README 原文：*"drpy2 兼容层 | `rt.load2x()` 老源零改动运行（伪全局映射 + 自动串行）"*。

---

## 二、改动文件

### 新增

| 文件 | 说明 |
|---|---|
| `src-tauri/vendor/drpy3-muhai.bundle.js` | **构建产物**（1.7MB，esbuild IIFE）。drpy3 引擎 + peer 链 + 库包 + cheerio 版 jsoup + 幕海胶水 + QuickJS 全局垫片，单文件自包含，QuickJS 直接 `eval` |
| `src-tauri/vendor/quickjs-pre.js` | 垫片源码 · 第一层（零依赖）：atob/btoa、crypto、performance、queueMicrotask、TextEncoder/TextDecoder、`Uint8Array.fromBase64`、`ArrayBuffer.prototype.resizable`、WebAssembly 空壳 |
| `src-tauri/vendor/quickjs-shims.js` | 垫片源码 · 第二层：装载 whatwg-url 的 `URL` / `URLSearchParams` |
| `src-tauri/vendor/glue.mjs` | 胶水源：HostEnv 注入（req / syncReq / pdfh / pdfa / pd / pdfl / store / log / getProxy / loadAsset）+ 六环节调度面 |
| `src-tauri/vendor/build.mjs` | esbuild 打包脚本（`node build.mjs`，改引擎或胶水后重跑） |
| `src-tauri/src/drpy3.rs` | Rust 侧引擎宿主：专用线程 + bundle 常驻 + 同步 HTTP 桥 + Promise 手动泵 |
| `src/engine/adapters/drpy3.ts` | 前端适配器：drpy 规则识别 + TVBox 结构 → 幕海 `MediaItem` |

### 修改

| 文件 | 说明 |
|---|---|
| `src-tauri/Cargo.toml` | `reqwest` 增加 `charset` 特性（按响应头 charset 解码，gbk 站点需要） |
| `src-tauri/src/lib.rs` | `mod drpy3;` + 新增 `drpy3run` 命令并注册（`spawn_blocking` 包一层，避免占死 tokio worker） |
| `src/engine/adapters/js.ts` | 加载脚本后判定是否 drpy 规则，是则整体转派 `createDrpy3Source`（search / home / getDetail / getPlayUrl / test 五个入口） |
| `package.json` / `src-tauri/tauri.conf.json` / `src-tauri/Cargo.toml` / `README.md` | 版本号 3.5.8 → **3.6.0** |
| `CHANGELOG.md` | 追加 V3.6.0 段落 |

---

## 三、关键技术决策

### 1. 异步档位选「档 C + 手动泵 job」
QuickJS 同步宿主没有事件循环，而 drpy3 内部大量 `await`。做法：Rust 侧执行表达式拿到 Promise 后，
循环 `ctx.execute_pending_job()` 直到 `__done` 置位（上限 10 万次防死循环）。
因为 `req` 是**同步阻塞**的（reqwest::blocking），所有 Promise 都能在泵的过程中 settle，无需真异步运行时。

### 2. 引擎固定在专用 OS 线程
QuickJS 单线程亲和。引擎（Runtime + Context + 已装载的源实例）常驻在一条 16MB 栈的线程上，
调用方经 `mpsc` 投递 job。好处：bundle 只 eval 一次（实测 210ms），后续调用 2ms 起；
也避免了跨线程复用 Runtime 的隐患。

### 3. 不注入 `evalModule`
源走引擎的「中性形态」求值（`AsyncFunction` 包裹，要求源有 `export default`），
避免让 QuickJS 实现动态 `import`。含相对 `import` 的源需先做「模式 B 预打包」（`node cli/drpy3-build.mjs 源.js`）。

### 4. 两层垫片（踩坑记录）
第一版把 URL polyfill 和其它垫片放同一模块 → `whatwg-url` 的初始化排在模块体之前，
而它初始化时要读 `ArrayBuffer.prototype.resizable`（QuickJS 没实现）→ 直接抛错。
拆成两层后：零依赖层先跑补齐底层全局，第二层才敢装 whatwg-url。

另一个坑：`typeof 未声明变量` 不抛错而返回 `"undefined"`——当时把 `import {URL as PolyURL}` 留在了 glue.mjs，
shims 里 `PolyURL` 成了未声明全局变量，条件静默为 false，URL 一直没装上，
表现是「详情 URL 变成 `host + detailUrl` 的畸形拼接」。已修。

---

## 四、验证结果

### Node 侧（同步 curl 桥，模拟档 C）
| 环节 | drpy3 原生源（百忙无果 dr3） | drpy2 老源（百忙无果[官] dr2，走 load2x） |
|---|---|---|
| init | ✓ | ✓ |
| home | ✓ 7 个分类 | ✓ 7 个分类 |
| category | ✓ 20 条真实数据 | ✓ 20 条真实数据 |
| detail | ✓ 带 `vod_play_from/url` | ✓ 带 `vod_play_from/url` |
| play | ✓ `{parse:1,url:...,flag:"mgtv"}` | ✓ 同上 |
| search | 空列表（mgtv 搜索接口 403） | 报错（同上，站点风控） |

### Rust 侧（reqwest 真网，`/tmp/drpy3test`）
```
✓ init     (214ms，含 bundle 冷启动)
✓ home     (2ms)
✓ category (430ms, 12KB)
✓ detail   (2.5KB，含真实选集)
✓ play     {"parse":1,"url":"https://www.mgtv.com/b/778406/24589329.html","flag":"mgtv"}
```

### 构建
- `pnpm build` ✓（108 modules）
- `cargo check` ✓（无新增 error/warning）

---

## 五、怎么用（用户侧）

在「导入源 / 仓库」里添加 JS 脚本源，脚本地址指向 drpy 规则文件即可（`.js`），
引擎会自动识别并转派。若识别不准，可在源配置里显式写：

```json
{ "type": "js", "name": "某站", "api": "https://.../xxx.js", "extra": { "engine": "drpy3" } }
```

`extra.engine` 取值：`drpy3`（强制新引擎）/ `legacy`（强制旧沙箱）/ 不写（自动识别）。

---

## 六、已知限制

1. **含相对 `import` 的模块化源**暂不能直接跑，需先预打包（模式 B）。
2. **wasm 解密类源**：QuickJS 无原生 WebAssembly，`capabilities.wasm` 报 `none`，此类源会降级或失败。
3. **随源资产**（`loadAsset` 读 wasm/模块文件）未实现，返回空。
4. **search 风控**：与引擎无关，取决于源站是否放行出口 IP。
5. 安装包体积 +约 1.7MB（bundle）+ encoding_rs（charset 特性）。
