# 幕海 V3.6.1 更改清单（P0 收尾：drpy 聚合源可直接导入）

> 版本号：3.6.0 → **3.6.1**
> 目标：修复 V3.6.0 接入 drpy3 引擎后，**用户仍无法把 drpy 聚合源导入 App** 的问题，让 P0 真正可用。

## 背景（为什么这版必须做）

V3.6.0 的 drpy3 引擎在 Rust/Node 真网验证里六环节全通，但用户实测导入订阅时却报
「未在该页面识别到可用的源配置」。定位到两个根因：

1. **聚合配置被整体吞掉**：TVBox 聚合（`{sites:[...]}`）进入 `parseFetched` 后，`isTvboxConfig`
   直接把整份当成「一个」tvbox 源（`baseUrl` 指向 JSON 自身），里面的 drpy 子站根本没被展开成独立源。
2. **内联规则被注释清洗破坏**：把 drpy JS 规则内联进 JSON 后，App 的 `stripJsonComments` 会把规则里的
   `//`、`/* */` 当注释删掉，JSON 结构被破坏，`JSON.parse` 在规则处抛错 → 落到 HTML 链接提取分支 → 报错。

## 改动

### 前端 `src/lib/sourceFetch.ts`
- 新增 `expandSites(data)`：导入含 `sites[]` 的配置时，把每个 **drpy 规则源**展开成独立的 `type:'js'` 源：
  - 判定：`spider`（内联规则）/ `spiderUrl`（`.js`）/ `api=框架 + ext=.js规则` 三种形态。
  - 加载入口用 `ext` 或远程规则 URL 当 `spiderUrl`（框架由 drpy3 引擎自带，忽略 TVBox 的 `api` 框架字段）。
  - `csp_*` Dex 蜘蛛确定跑不了，自动跳过；纯 tvbox 聚合无 drpy 站点时仍保持「整体单源」旧行为，兼容现有订阅。
  - drpy 源补 `baseUrl`（用规则 URL 占位），以满足 `store.importSources` 的 `type+baseUrl` 过滤与去重。
- `parseFetched` 的 tvbox 分支：`isTvboxConfig` 命中后**优先** `expandSites`，有 drpy 源则返回展开结果。
- `parsePasted`：手动粘贴含 `sites` 的 JSON 时同样优先展开 drpy 站点。

### 源仓库（Gitee `xmyzjxn/muhai-vod`）
- `drpy-sources.json` 改为**不内联**：芒果TV 规则提取为 `rules/mgtv-dr2.js`（远程加载），避开注释清洗雷区。
- 新增虎牙 / 斗鱼 / 兔小贝 3 个**直播** drpy2 源（来自用户 `ysc.txt`，规则经 gh-proxy / git.yylx.win 代理实测可达）。
- 当前共 5 源：**2 点播（豆瓣 / 芒果） + 3 直播（虎牙 / 斗鱼 / 兔小贝）**。
- README 同步更新 drpy 章节（5 源表格 + V3.6.1 自动展开说明），`虎牙/斗鱼/兔小贝` 标记已加回，A3 待办标记完成。

### 版本号
- `package.json` / `src-tauri/tauri.conf.json` / `src-tauri/Cargo.toml` / `README.md`：3.6.0 → 3.6.1
- `CHANGELOG.md`：新增 V3.6.1 段落（本文件为权威清单的精简版）。

## 验证（出包前已做）

| 项 | 方法 | 结果 |
|---|---|---|
| 展开逻辑 | node 复现 `expandSites` | `drpy-sources.json` → 5 个 js 源；`ysc.txt`（48 站）→ 3 个 drpy 直播源、跳过 45 csp |
| gitee raw 可达 | curl 抓 raw | `drpy-sources.json`、`rules/mgtv-dr2.js` 均 `HTTP 200 / text/plain` |
| 类型检查 | `npx tsc --noEmit` | `sourceFetch.ts` 无新增错误（既有 `AddSourceModal`/`netdiskLogin` 类型错误与本次无关，`build` 脚本为 `vite build` 不卡 tsc） |

## 用户操作（导入后）

App「音源管理 → 添加源」→ 类型选「订阅 / TVBox」→ 粘贴：

```
https://gitee.com/xmyzjxn/muhai-vod/raw/master/drpy-sources.json
```

V3.6.1 会自动展开成 5 个独立子站。点播源（豆瓣 / 芒果）可直接验证 P0 链路；直播源（虎牙 / 斗鱼 / 兔小贝）
能否流畅播放取决于直播流协议（HLS/m3u8 通常可，flv 可能不行），请在 App 内实测。

## 已知限制

- **`csp_*` Dex 蜘蛛仍不可用**：用户 `ysc.txt` 里 45 个 csp 源、以及清单里王二小/俊哥/饭太硬等，是编译成
  Android 原生代码的蜘蛛，幕海 Tauri+QuickJS（纯 JS 引擎）架构上跑不了，非本版范围。
- **drpy3 原生（`export default`）规则暂未收录**：当前 `catvodize` 会剥离顶层 `export`，需后续 `js.ts` 适配。
- **直播源稳定性依赖代理**：虎牙用 gh-proxy.com、斗鱼/兔小贝用 git.yylx.win，代理可用性随网络环境变化。
