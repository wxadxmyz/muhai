# V3.6.2 更改清单

## 问题现象
安卓端（V3.6.1）导入 `drpy-sources.json` 后，搜索任何 drpy 源都提示：

> 该源未连通  
> drpy3 search 调用失败: Command drpy3run not allowed by ACL

## 根因
Tauri 2 在移动端严格执行命令 ACL。`drpy3run` 虽然在 `src-tauri/src/lib.rs` 的 `generate_handler!` 中注册，但**未在 capability / permission 清单中显式放行**，因此安卓端直接拒绝调用。桌面端 ACL 默认宽松，所以 V3.6.0/V3.6.1 的桌面构建没暴露这个问题。

## 修复内容

| 文件 | 改动 |
|---|---|
| `src-tauri/permissions/autogenerated/drpy3run.toml` | 新增 permission 定义：`allow-drpy3run` / `deny-drpy3run` |
| `src-tauri/capabilities/default.json` | `permissions` 数组追加 `"allow-drpy3run"`（android/iOS） |
| `src-tauri/capabilities/desktop.json` | `permissions` 数组追加 `"allow-drpy3run"`（桌面端统一） |
| `package.json` / `src-tauri/tauri.conf.json` / `src-tauri/Cargo.toml` / `README.md` | 版本号 3.6.1 → 3.6.2 |
| `CHANGELOG.md` | 新增 V3.6.2 段落 |

## 验证
- `cargo check` 通过，Tauri 不再报 `Permission allow-drpy3run not found`。
- 仅 3 个预存 warning（`unused_mut` / `unused_variable`），非本版引入。

## 用法不变
导入地址仍为：
```
https://gitee.com/xmyzjxn/muhai-vod/raw/master/drpy-sources.json
```
V3.6.2 会正确展开为 5 个 drpy 子站（2 点播 + 3 直播），搜索/首页/详情/播放不再被 ACL 拦截。
