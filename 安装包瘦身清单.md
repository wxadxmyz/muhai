# 幕海安装包瘦身清单

> **状态**：✅ 已于 **V3.8.4** 与 drpy 修复一起执行（项①②③全上）。详见 `CHANGELOG.md` 的 V3.8.4 条目。

> **适用版本**：V3.8.3（删 csp 体系 + 修播放链路）发布并真机验证通过之后，作为下一个版本（暂定 V3.8.4 / V3.9.0）执行。
> **目标**：把 Android APK 从当前约 **99 MB**（4 个 ABI 打进一个包）显著降下来，同时不破坏桌面端构建。
> **前置条件**：本清单**不依赖** V3.8.3 的删 csp 工作——但建议先做完 V3.8.3，避免体积问题 + 功能问题混在一起排查。

---

## 当前体积构成（为什么是 99 MB）

| 因素 | 贡献 | 说明 |
|---|---|---|
| 4 个 ABI 打进同一个 APK | **主因（≈70 MB 是冗余）** | CI 用 `tauri android build --apk` 默认编 `arm64-v8a / armeabi-v7a / x86 / x86_64`，各自带一份 `libapp_lib.so`，塞进一个 universal APK |
| `tauri-plugin-shell` 插件 | **+10 MB** | V3.7.7 引入，前端已零引用（`@tauri-apps/plugin-shell` 无 import），纯浪费 |
| 无 `[profile.release]` 优化 | 中等 | Rust 默认 release 是 `opt-level=3` 但不开 `lto` / `strip`，.so 体积偏肥 |
| 其他 | 少量 | 图标、资源、WebView（系统提供，不占 APK） |

**关键结论**：体积大头是"4 份 so 重复打包"，其次是 shell 插件和缺编译优化。下面三项正好对应这三处。

---

## 项①：移除 `tauri-plugin-shell` 插件（低风险，必做）

> 风险：低。前端零引用，删了等于把 V3.7.7 误加的 +10 MB 还回去。

### 改动位置与具体改法

**1. `src-tauri/Cargo.toml` 第 40 行** —— 删除该行

```toml
# 删除这一行 ↓
tauri-plugin-shell = "2"
```

**2. `src-tauri/src/lib.rs` 第 229 行** —— 删除该行

```rust
// 删除这一行 ↓
.plugin(tauri_plugin_shell::init());
```

> 注意：第 229 行后面紧跟 `#[cfg(desktop)] {` 块，删掉 `.plugin(tauri_plugin_shell::init());` 后，上面的链式调用 `... .plugin(tauri_plugin_notification::init())` 作为链尾，结尾的分号保留即可，builder 继续 `let mut builder = ...` 正常编译。

**3. 前端引用核查（删前必做）**

```bash
# 确认前端没有 import shell 插件（历史核查为 0 引用，再确认一次）
grep -rn "@tauri-apps/plugin-shell" src/ || echo "OK: 前端零引用"
```

### 预期收益

- APK 约 **-10 MB**（99 → ~89 MB，此时仍未分包，仍是 universal）。

### 验证

- [ ] `grep` 确认前端无 shell import
- [ ] `cargo check`（Android target）通过
- [ ] 桌面端 `pnpm tauri build` 通过，启动后对话框/文件/通知/自启/快捷键/更新均正常（这些插件与 shell 无关）

---

## 项②：加 `[profile.release]` 编译优化（中风险，建议保守开）

> 风险：中。会改变 .so 体积与编译时长；激进选项（如 `panic="abort"`）会丢失崩溃 backtrace，初期不利排障。

### 改动位置与具体改法

**`src-tauri/Cargo.toml` 末尾追加**（放在文件最后，与 `[package]` 等并列）：

```toml
# ===== 体积优化：release 编译配置 =====
[profile.release]
opt-level = "z"      # 最小体积优先（而非最快 z3）
lto = true           # 链接时优化，跨 crate 内联/裁剪
strip = true         # 去掉符号表与调试信息
```

**本轮先不加（留到后续版本）**：

```toml
# codegen-units = 1   # 编译极慢，CI 可能超时，且对体积收益有限，先不上
# panic = "abort"     # 崩溃不再有 backtrace，初期排障不利，待稳定后再评估
```

### 预期收益

- 每份 `libapp_lib.so` 再小 **20%~25%**。
- 与项①叠加（仍为 universal 包）：99 → ~89 → **~65~75 MB**。

### 风险与对策

- **CI 编译变慢**：`lto=true` 显著增加链接时间。对策：先在 CI 跑一次确认不超时（GitHub Actions 默认 6 小时上限，通常够）。
- **strip 后无法直接 gdb**：崩溃排查靠 Rust panic 信息（stdout/log）而非符号，足够日常。

### 验证

- [ ] `cargo build --release`（桌面）通过，binary 体积对比优化前下降
- [ ] CI 构建时长未超历史均值过多（关注 `--release` 链接阶段）
- [ ] 真机启动、搜索、播放正常（确认 strip/lto 没裁掉必需符号导致运行时崩溃）

---

## 项③：`--split-per-abi` 按架构分包（高收益，高风险，单独版本验证）

> 风险：高。分发方式改变——一个 APK 变多个 ABI APK，需要改发布页与下载指引。

### 改动位置与具体改法

**方案 A（推荐，改 CI 命令，不碰配置）**：`.github/workflows/android.yml` **第 578 行**

```bash
# 原命令
pnpm tauri android build --apk 2>&1 | tee /tmp/build.log

# 改为
pnpm tauri android build --apk --split-per-abi 2>&1 | tee /tmp/build.log
```

**方案 B（改配置，等效）**：`src-tauri/tauri.conf.json` 第 58 行 `android` 段加字段

```json
"android": {
  "minSdkVersion": 24,
  "splitPerAbi": true
}
```

> 推荐 **方案 A**：用 CLI flag，改动集中在一行，将来想关掉直接去掉 flag，不污染 `tauri.conf.json`（那份文件桌面/CI 共用，少动为好）。

### 分包后产物变化

| 架构 | 现代手机覆盖 | 是否真机需要 |
|---|---|---|
| `arm64-v8a`（aarch64） | 99% 现代手机 | **必发** |
| `armeabi-v7a`（armv7） | 老款 32 位手机 | 建议发（兜底老机） |
| `x86` / `x86_64` | 仅模拟器 | **可丢弃**（真机无此架构） |

- 开启 `--split-per-abi` 后，CI 产出按架构分离的 APK（不再是单一 universal）。
- 单个 `arm64-v8a` APK 体积约 **20~25 MB**（已含项①②优化），对比原 99 MB 下降约 **75%**。

### 发布侧必改（高风险点）

- [ ] **下载页改多链接**：提供「arm64-v8a（推荐）」「armeabi-v7a（老手机）」两个下载按钮；不再有单一"通用 APK"。
- [ ] **用户指引**：页面注明"不知道机型选 arm64-v8a"；老手机（2017 年前 / 32 位）选 armeabi-v7a。
- [ ] **可选 universal 兜底**：若担心用户选错，可额外用不带 `--split-per-abi` 的命令再编一次，产出一个 universal APK 标「其他/通用」，但会增加约一倍 CI 时间，按需取舍。
- [ ] **更新通道**：若用了 `tauri-plugin-updater`，确认其按当前架构推送对应 APK，避免 x86 模拟器包推给真机。

### 验证

- [ ] CI 产物从 1 个 APK 变为多个 ABI APK（确认文件名含 `arm64-v8a` / `armeabi-v7a`）
- [ ] 真机（arm64）安装对应 APK，启动/搜索/播放全流程正常
- [ ] 老手机（armv7）安装对应 APK 正常（若有设备可测）
- [ ] 下载页链接与指引更新到位

---

## 执行顺序与版本规划建议

| 阶段 | 版本 | 内容 | 为什么分开 |
|---|---|---|---|
| 1 | **V3.8.3** | 删 csp 体系 + 修播放链路 | 功能修复，先稳 |
| 2 | **V3.8.4（瘦身）** | 项①② + 项③ | 纯体积优化，单独验证避免与功能问题混查 |

**建议 V3.8.4 内部也分步**：
1. 先上项① + 项②（不分包），出一个 APK（~65~75 MB），你装上确认功能/体积 OK。
2. 再上项③（分包），单独验证分发与安装，避免一步到位出问题难定位。

---

## 最终体积预期（三项全上）

| 方案 | 产物 | 预计体积 |
|---|---|---|
| 仅项①②（不分包） | universal APK | **~65~75 MB** |
| 项①②③全上 | arm64-v8a APK | **~20~25 MB** |

> 注：以上为基于当前 99 MB 构成的理论估算，实际以 V3.8.4 真机构建为准。drpy 引擎（QuickJS / `drpy3.rs`）与本地媒体代理（hyper）**不删**，它们贡献的体积是必要的播放能力。
