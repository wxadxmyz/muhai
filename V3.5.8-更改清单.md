# 幕海 MuHai V3.5.8 更改清单（草稿 · 待动手）

> 对应 2026-09-20 反馈的 5 处 UI 问题 + 点播源仓库瘦身。
> **当前仅完成实测与代码定位，尚未改源码、未构建。**
> 版本号待升：`3.5.8`（`package.json` / `src-tauri/tauri.conf.json` / `src-tauri/Cargo.toml` 三处同步）。

---

## 〇 · 点播源仓库瘦身（Gitee `xmyzjxn/muhai-vod`）

**判定标准**：`ac=detail&wd=<关键词>` 是否真能返回影片。实测 22 个子站，结果如下。

### 删除清单（6 个）

| # | 站点 | API | 删除原因 |
|---|---|---|---|
| 1 | 茅台资源 | `https://caiji.maotaizy.cc/api.php/provide/vod/` | 搜索接口返回 `code:1002 / Current API forbids keyword search`，**官方禁用关键词搜索** |
| 2 | 索尼资源 | `https://suoniapi.com/api.php/provide/vod/` | 搜索请求 403；换 4 种 UA 均 403。列表接口正常 → **服务端只封搜索通道** |
| 3 | 闪电资源 | `https://xsd.sdzyapi.com/api.php/provide/vod/` | 返回纯文本 `暂不支持搜索`（`ac=videolist`/`ac=search` 同样） |
| 4 | 鸭鸭资源 | `https://cj.yayazy.net/api.php/provide/vod/` | 同上，`暂不支持搜索` |
| 5 | 牛牛资源 | `https://api.niuniuzy.me/api.php/provide/vod/` | 同上，`暂不支持搜索` |
| 6 | 无尽资源 | `https://api.wujinapi.com/api.php/provide/vod/` | **响应极慢**：`ac=list` 读超时；`wd=庆余年` 重试可出 9 条但 `wd=你好` 又超时。可用性不稳定 |

> 说明：前 5 个站的**片库是存在的、列表接口也活着**，只是搜索通道被服务端关闭。
> 但按你的决定——**放在 App 里点进去永远 0 条，直接删掉**。

### 保留清单（16 个）

| # | 站点 | 实测命中 |
|---|---|---|
| 1 | 豪华资源 | 8 |
| 2 | 红牛资源 | 8 |
| 3 | 光速资源 | 8 |
| 4 | LZ资源站 | 3 |
| 5 | 360资源 | 3 |
| 6 | 金鹰资源 | 8 |
| 7 | 暴风资源 | 10 |
| 8 | 最大资源 | 7 |
| 9 | 极速资源 | 8 |
| 10 | 虎牙资源 | 8 |
| 11 | 百度资源 | 2 |
| 12 | 速博资源 | 8 |
| 13 | 魔都资源 | 3 |
| 14 | 非凡资源 | 8 |
| 15 | 飘零资源 | 8 |
| 16 | 魔都资源(主) | 3 |

**落地动作**：`sources.json` 22 → 16 条；同步更新 `README.md` 站点表格。

---

## 问题 1 · 搜索源列表后面的「0」胶囊 —— **仅删源，不改代码**

**现象**：搜索「影视」时，左侧子站列表里有源在名字后跟一个灰色胶囊「0」。这些正是**没有搜索能力**的源。

**决策（已确认 2026-09-20）**：**只把 Gitee 源仓库里搜不到影片的站点删掉（见「〇」），前端代码不动。**

> 前端根因仅作备案，本次**不实施**：
> - `src/components/SearchView.tsx` 第 11–14 行源状态为三态 `ok` / `empty` / `error`；
> - 第 282 行 `{st?.kind === 'ok' ? st.count : 0}` 让 `empty` 落到兜底分支，渲染出「0」。
>
> 删完那 6 个站后，正常情况下不会再出现 `empty` 态，问题即消失。
> 若将来某个源被封搜索，「0」会复活——届时可再单独修这一行。

---

## 问题 2 · 左侧子站列表最底下一个显示一半

**根因**（`src/styles.css`）
- 第 1264–1278 行：
  ```css
  .search-source { height: 30px; }   /* 写死高度，但没禁 flex 压缩 */
  ```
  它是 `.search-sources`（flex column）的子项。容器高度不是 30px 整数倍时，**最后一条会被 flex 压扁**。
- 第 1250–1262 行：
  ```css
  .search-sources { …; padding: 6px 6px; }   /* 底部只 6px，无滚动缓冲 */
  ```
  滚到底时最后一条贴边，视觉上像被切一半。
- 现有 22 个站（删减后 16 个），116px 固定宽列在手机上必然滚动，问题被放大。

**改法**
```css
.search-sources { padding: 6px 6px 16px; }        /* 底部留滚动尾巴 */
.search-source  { flex: 0 0 auto; }               /* 禁止被压缩 */
```

---

## 问题 3 · 横屏设置浮窗太宽 → 统一成一档

**现象**：横屏点「设置」弹出的浮窗比弹幕按钮弹出的宽。

**根因**：横屏浮层宽度写成了**四套不同的数字**，视觉不统一。

| 浮层 | 当前宽度 | 位置 | 类别 |
|---|---|---|---|
| 弹幕 / 外挂字幕样式 | `auto`（min 320 / max 400px） | `styles.css:778` | `.vp-sub-drawer` ← **基准** |
| 横屏选集 | `74%` | `styles.css:2239` | `.epsheet.land-ep-sheet` |
| **横屏设置** | **`76%`**（min 300 / max 340） | `styles.css:2257` | `.drawer` |
| 直播横屏选台 | `78%` | `styles.css:1543` | `.epsheet.live-pick-sheet` |

**改法**：四套统一为弹幕面板同一档 —— `width: auto; min-width: 320px; max-width: 400px;`

- `styles.css:2257` `.player-root.landscape .drawer` → `width:auto; min-width:320px; max-width:400px`
- `styles.css:2239` `.player-root .epsheet.land-ep-sheet` → `width:auto; min-width:320px; max-width:400px`
- `styles.css:1543` `.land-overlay .epsheet.live-pick-sheet` → `width:auto; min-width:320px; max-width:400px`

> **已确认接受**：定宽后，选台面板里长频道名会被 `text-overflow: ellipsis` 截断。

---

## 问题 4 · 选台浮窗上滑，亮度/音量跟着滑（手势穿透）★ 功能性 Bug

**现象**：点播/直播横屏，点「选台」弹出浮层，在浮层里向上滑 → 亮度或音量 HUD 跟着弹出。

**根因**（`src/video/views/Live.tsx`）
- 浮层 DOM **写在手势容器的内部**：
  - 第 733–740 行：容器开局 `<div className="land-overlay landscape" onTouchStart={…} onTouchMove={…} onTouchEnd={…}>`
  - 第 798–799 行：浮层在**容器内**
    ```tsx
    <div className="drawer-mask open" onClick={…} onTouchEnd={(e) => e.stopPropagation()}>
      <div className="epsheet open live-pick-sheet" onClick={…} onTouchEnd={(e) => e.stopPropagation()}>
    ```
- **只挡了 `onTouchEnd`**（第 798/799、818/819 行）。
  `touchstart` / `touchmove` 一路冒泡到容器，被 `onStageTouchStart`（323 行）/ `onStageTouchMove`（333 行）当成屏幕手势处理：
  ```ts
  // 334–357 行
  if (Math.abs(dy) > Math.abs(dx)) {
    const start = el.__half === 'left' ? el.__bStart : el.__vStart;   // 左半=亮度 / 右半=音量
    …
    showHud('bright' | 'vol', …);
  }
  ```

**连带问题（同一根因）**：选台面板**内部无法上下滚动** —— 在里面滑，滑的是亮度而不是频道列表。

**改法**
1. 两处浮层（选台 `798` / 换源条 `818`）补拦截：
   ```tsx
   onTouchStart={(e) => e.stopPropagation()}
   onTouchMove={(e) => e.stopPropagation()}
   onTouchEnd={(e) => e.stopPropagation()}
   ```
2. `.live-pick-sheet` / `.live-srcbar-sheet` 加 `touch-action: pan-y`，让面板自己滚。

---

## 问题 5 · 直播回看进度条只能点、不能滑

**根因**（`src/video/views/Live.tsx`）
- 第 772 行：**只绑了 `onClick`**
  ```tsx
  <div className="ts-bar" onClick={onTsClick}>
  ```
- `onTsClick`（第 257–271 行）只从 `e.clientX` 取一次比例就 `currentTime = t`，**没有任何 drag 处理**。
- `styles.css:1993`：轨道**只有 4px 高**，手指按不准。

**改法**
1. 改用 pointer 事件做拖动：`pointerdown` 取点 → `pointermove` 跟手 seek → `pointerup` 结束，
   并复用 `onTsClick` 里的 seekable 取值逻辑（抽成 `seekByRatio(ratio)`）。
2. 热区扩到 ~20px，视觉仍 4px（透明 padding 或伪元素扩热区）：
   ```css
   .ts-bar { height: 20px; background: transparent; }   /* 热区 */
   .ts-bar::before { /* 4px 视觉轨道 */ }
   ```

---

## 改动汇总

| # | 问题 | 文件 | 位置 | 性质 |
|---|---|---|---|---|
| 0+1 | 删 6 个无搜索能力源（问题 1 由此解决） | Gitee `sources.json` + `README.md` | — | 数据 |
| 2 | 源列表底部半个 | `styles.css` | 1250 / 1267 | 视觉 |
| 3 | 横屏浮层宽度统一 | `styles.css` | 778 / 1543 / 2239 / 2257 | 视觉 |
| 4 | 选台浮层手势穿透 ★ | `Live.tsx` + `styles.css` | 798 / 818 / 1543 | **功能** |
| 5 | 回看进度条不能拖 | `Live.tsx` + `styles.css` | 772 / 257 / 1993 | **功能** |

> 问题 1 **不改代码**，仅通过删源解决（见上）。前端 `SearchView.tsx:282` 的「0」兜底分支保留原样。

**风险**：除问题 4 外均为低风险视觉对齐；问题 4/5 涉及事件流，改动后需真机回归：
- 横屏左右滑切台仍生效
- 浮层内可滚动、外部手势不误触
- 回看拖动后「回到直播」按钮仍可跳回边缘
