# V3.4.0 更改清单 —— 横屏弹幕按钮交互根治 + 工具栏文字显色

> 版本：V3.4.0（从 V3.3.13 升级）
> 关联背景：V3.3.13 把横屏左中弹幕按钮改成纯 onClick 后，在用户机型的 WebView 上点击派发不到，导致「关不了弹幕」；同时给底部「弹幕」按钮加的 onTouchStart 会被面板遮罩的合成 click 秒关（闪一下）。

---

## 一、真机现象（用户反馈）

1. 横屏 + 竖屏的弹幕开关按钮（点一下开/点一下关）全部处于「开启态高亮」，点一下**关不掉、也打不开**。
2. 横屏底部工具栏「弹幕」按钮：轻触后**偶尔闪一下**，面板出不来。
3. 横屏底部工具栏 10 个按钮里，开启态（`.on`）的按钮（如弹幕、解码、片头）文字是**蓝紫色**，普通按钮是**白色**，视觉不一致；蓝紫字在深色视频背景上对比度低，**看着不显**。

---

## 二、根因

### 2.1 左中弹幕按钮关不掉
- V3.3.13 把左中弹幕图标从「长按唤出面板」改成「纯 `onClick={toggleDanmaku}` 开关」。
- 但用户机型的 WebView 对播放器内图标的**合成 click 派发不可靠**（与更早的「底部弹幕按钮点了没反应」同源）——`onClick` 收不到，所以 `toggleDanmaku()` 不执行，弹幕状态卡在开启。
- 证据：长按（touch 事件）在旧版能用、纯 click 在新版失灵，说明是 click 合成层面的问题，不是逻辑错。

### 2.2 底部「弹幕」按钮闪一下
- V3.3.13 给底部按钮加了 `onTouchStart` 直接开面板（绕开 click 派发问题）。
- 手指按下 → 面板打开 → 面板是**全屏遮罩 `.vp-drawer-mask`**，立刻盖住工具栏。
- 手指抬起时，浏览器把这次触摸**合成成 click**，目标落在刚盖到顶层的遮罩上 → 遮罩 `onClick={() => setShowSubStyle(false)}` 立刻关闭面板 → 表现为「闪一下」。

### 2.3 工具栏文字不显
- `.landscape .tools button.on` 用 `color: var(--accent)`（蓝紫）+ 淡蓝紫背景，在深色背景下对比不足。

---

## 三、改动明细（代码级）

### 改动 1：竖屏左中弹幕按钮 —— 补 touch 触发（文件 `src/video/VideoPlayer.tsx`）
- 位置：约 1379 行
- 原：`onClick={toggleDanmaku}`
- 改：`onClick={danmakuClickToggle} onTouchStart={danmakuTouchToggle}`
- 目的：触摸设备也能稳定开关弹幕（onClick 收不到时 touch 能补上）。
- ⚠️ **去重关键**：真机一次轻触会先走 `onTouchStart` 再合成 `onClick`，若两个都直接 `toggleDanmaku` 会被「开→关」抵消。
  因此用 `danmakuTouchFired` 标记位：`danmakuTouchToggle` 触发后置位 600ms，`danmakuClickToggle` 见到标记位直接吞掉。
  桌面端无 touch，只有 click，标记位恒 false，正常切换。

### 改动 2：横屏左中弹幕按钮 —— 补 touch 触发
- 位置：约 1411 行（V3.3.13 改成的纯开关按钮）
- 原：`onClick={toggleDanmaku}`
- 改：`onClick={danmakuClickToggle} onTouchStart={danmakuTouchToggle}`
- 目的：同上，修复「关不了」。去重逻辑同改动 1。

### 改动 3：横屏底部「弹幕」按钮 + 遮罩 —— 防同手势秒关面板
- 底部按钮（约 1459 行）：
  - 原：`onClick={() => setShowSubStyle(true)} onTouchStart={() => setShowSubStyle(true)}`
  - 改：`onClick={openSubStyle} onTouchStart={openSubStyle}`
  - `openSubStyle = () => { subStyleOpenAt.current = Date.now(); setShowSubStyle(true); }`
- 遮罩关闭（弹幕面板 + 外挂字幕面板）：
  - 原：`onClick={() => setShowSubStyle(false)}` / `setShowSubtitleStyle(false)`
  - 改：`onClick={() => { if (Date.now() - subStyleOpenAt.current < 400) return; setShowSubStyle(false); }}`
- 目的：**不靠 `e.preventDefault()`**（React 的 `onTouchStart` 为 passive 监听，`preventDefault` 不生效）；
  改为「打开瞬间记时间戳 + 遮罩 400ms 内忽略点击」——同一手势抬手合成的 click 落在遮罩上会被忽略，面板稳留；你主动再次点遮罩仍正常关闭。

### 改动 4：横屏底部工具栏 `.on` 文字显色（文件 `src/styles.css`）✅ 已按预览方案落地
- 位置：1723 行 `.landscape .tools button.on`
- 原：`background: color-mix(in srgb, var(--accent) 20%, transparent); color: var(--accent);`
- 改：
  ```css
  .landscape .tools button.on {
    background: color-mix(in srgb, var(--accent) 42%, transparent);
    color: #fff;
    font-weight: 700;
  }
  ```
- 目的：开启态与关闭态统一为白字、加深蓝紫底、加粗，对比度提升、整排视觉一致。

---

## 四、版本号

- `package.json` → 3.4.0
- `src-tauri/tauri.conf.json` → 3.4.0
- `src-tauri/Cargo.toml` → 3.4.0

> 跳到 V3.4.0 作为「播放器横屏交互 + 面板 UI 定稿」里程碑版本。

---

## 五、真机验收（5 条）

1. **竖屏左中弹幕按钮**：轻触开、再轻触关，状态随高亮切换（之前关不掉）。
2. **横屏左中弹幕按钮**：轻触开、再轻触关（之前关不掉）。
3. **横屏底部「弹幕」按钮**：轻触**稳定弹出面板**，不再闪一下、不再点不出。
4. **面板内「开启弹幕」开关**：点一下切状态，面板不消失。
5. **横屏底部工具栏**：开启态按钮文字清晰、与关闭态视觉协调（改动 4 生效时）。

---

## 六、状态

- 代码已改完，已通过 `tsc --noEmit`（`VideoPlayer` 零类型错误）与 `vite build`。
- 用户已确认改动 1–4 全部执行（含第 4 项「白字 + 加深蓝紫底」预览方案）。
- 已提交并打 tag `v3.4.0`，CI 构建中，APK 就绪后发链接。

> 下一步：真机验收六、五条的 5 项；全绿则手机版定稿，开工平板 UI + TV UI。
